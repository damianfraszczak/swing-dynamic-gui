package pl.edu.wat.wcy.swingdynamicgui.enums;

public enum FieldInputType {
    DEFAULT, COMBOBOX, SELECT, MULTI_SELECT, CHECKBOX, TOGGLE, DATE, DATE_TIME, TIME, CURRENCY,
    DOUBLE, INTEGER, PASSWORD;

}
